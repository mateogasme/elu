# @mgasme/elu-core

Framework-agnostic runtime for [elu](https://github.com/mateogasme/elu). Ships the controller, keyboard matrix, shared types, and CSS.

This package is an internal shared dependency consumed by `@mgasme/elu` (Astro), `@mgasme/elu-react` (React), `@mgasme/elu-vue` (Vue 3), and `@mgasme/elu-svelte` (Svelte 4/5). Install one of those packages instead.
